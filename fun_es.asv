function [f, root] = fun_es(x)
    
    % f(x):
    %f = x.^3 - 3 * x.^2 + 2;
    f = exp(x);
    % Si se solicita, encontrar la raíz usando bisección
    if nargout > 1
        syms z;
        fun_sym = z.^3 - 3 * z.^2 + 2;
        % Llamar a Biseccion con un intervalo que contenga una raíz
        [root, ~] = Biseccion(exp(z), 0, 5);
    end
end